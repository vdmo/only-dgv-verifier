[package]
name = "only-memory"
version = "0.1.0"
edition = "2021"

[dependencies]
only-core = { path = "../only-core" }
libm = "0.2"

[dev-dependencies]
only-evolution = { path = "../only-evolution" }
