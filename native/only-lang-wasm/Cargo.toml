[package]
name = "only-lang-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
only-lang = { path = "../only-lang" }
only-core = { path = "../only-core" }
getrandom = { version = "0.4", features = ["wasm_js"] }
