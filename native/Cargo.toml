[workspace]
members = [
    "only-core",
    "only-evolution",
    "only-memory",
    "only-lang",
    "only-lang-wasm",
    "dgv-verifier",
    "dgv-gate",
    "dgv-storage",
    "dgv-python",
    "only-gate",
    "only-proxy",
    "only-heal",
    "only-lang-ir",
]
resolver = "2"
