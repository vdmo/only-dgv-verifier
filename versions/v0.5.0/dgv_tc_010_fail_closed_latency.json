{
  "id": "DGV-TC-010",
  "svrnos_layer": "L7: Application Enforcement",
  "ger_mapping": "GER-501: Escalation Not Implemented",
  "latency_threshold_ms": 50,
  "claim_name": "Fail-Closed Latency",
  "claim_definition": "Verifies that if verification or gating operations exceed the allowed latency threshold (50ms), the system fails-closed immediately to protect downstream state.",
  "scope": "runtime CLI execution",
  "test_type": "exact-match",
  "test_cases": [
    {
      "id": "TC-LAT-001",
      "input": {
        "script": "harmony(1e-12)\nevolve(2)\ndata(1000)\nresidual()",
        "payload": 1000
      },
      "expected": {
        "gate_status": "CLOSED",
        "rejection_reason": "fail_closed_latency_timeout_exceeded"
      },
      "mandatory": true,
      "description": "Verifies that a simulated gating delay of 100ms triggers a fail-closed timeout event."
    }
  ],
  "metrics": [
    "gate_status",
    "rejection_reason"
  ],
  "pass_threshold": {
    "rejection_expected": true
  },
  "version": "0.4.0",
  "expiry": "2027-06-13T00:00:00Z"
}