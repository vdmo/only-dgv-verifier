# v0.7.0 release snapshot

This directory captures the v0.7.0 / 55-card release snapshot used by the binary-first verifier bundle.

The package is aligned with the current DGV registry and the native release artifacts under the package's binaries/ directory.
